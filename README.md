# 📚 Умный Конвертер Документов (Docling)

Автоматическая конвертация документов из различных форматов в Markdown с отслеживанием изменений.

## 🚀 Быстрый старт

1. Положите файлы в папку `input/`
2. Запустите: `python converter.py`
3. Результаты в папке `output/`

## 📁 Структура проекта
C:\VScode
├── input/ # Исходные документы
├── output/ # Результаты в Markdown
├── archive/ # Старые версии скриптов
├── venv/ # Виртуальное окружение
└── converter.py # Главный скрипт

## 🎯 Возможности

✅ **Автоматическое отслеживание** - обрабатывает только новые и изменённые файлы  
✅ **Поддержка форматов** - PDF, DOCX, PPTX, DOC, XLSX, HTML  
✅ **Извлечение таблиц** - таблицы конвертируются в Markdown  
✅ **OCR** - распознавание текста на изображениях  
✅ **Быстрая обработка** - 10-30 секунд на документ  

## 📝 Использование
Активировать окружение
Set-ExecutionPolicy Unrestricted -Scope Process
venv\Scripts\activate

Запустить конвертер
python converter.py

### Базовая конвертация

### Повторная обработка файла

1. Измените файл в `input/` (дата модификации обновится)
2. Запустите `python converter.py`

### Сброс базы обработанных файлов

Удалить базу (все файлы будут обработаны заново)
del .processing_log.json

text

## ⚙️ Что делает скрипт

1. Сканирует папку `input/`
2. Сравнивает с базой `.processing_log.json`
3. Обрабатывает новые и изменённые файлы
4. Сохраняет результаты в `output/`
5. Обновляет базу

## 🔧 Устранение проблем

### Ошибка: "Папка input не найдена"
mkdir input

text

### Ошибка: "Модели не загружены"
docling-tools models download

text

### Медленная первая обработка
Это нормально - модели загружаются (~700MB). Последующие запуски быстрые.

## 📊 Поддерживаемые форматы

| Формат | Расширение | Особенности |
|--------|-----------|-------------|
| PDF | `.pdf` | Таблицы, изображения, OCR |
| Word | `.docx`, `.doc` | Полная поддержка |
| PowerPoint | `.pptx` | Текст и таблицы |
| Excel | `.xlsx` | Таблицы |
| HTML | `.html` | Структура и текст |

## 🎓 Обучение

Это проект для изучения Docling от 0 до профи.  
Текущий этап: **Этап 2 завершён** ✅

---

Версия: 1.0  
Дата: 18.11.2025


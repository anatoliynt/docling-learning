# 📚 Умный Конвертер Документов (Docling)

Автоматическая конвертация документов из различных форматов в Markdown с отслеживанием изменений.

## 📦 Установка зависимостей

Перед первым использованием установите необходимые пакеты:

```bash
pip install -r requirements.txt
```

Или установите вручную:
```bash
pip install docling python-dotenv
```

После установки загрузите модели Docling:
```bash
docling-tools models download
```

## 🚀 Быстрый старт

### Способ 1: Батник (рекомендуется для Windows)

1. Положите файлы в `input/`
2. Двойной клик по `run.bat`
3. Результаты в `output/`

### Способ 2: PowerShell вручную
1. Положите файлы в `input/`
2. Двойной клик по `run.ps1`
3. Результаты в `output/`

### Способ 3: вручную через VScode
1. Положите файлы в папку `input/`
2. Запустите: `python converter.py`
3. Результаты в папке `output/`

## 📁 Структура проекта
```
docling-learning/
├── input/              # Исходные документы
├── output/             # Результаты в Markdown
├── archive/            # Старые версии скриптов
├── main_files/         # Основные файлы проекта
│   ├── converter.py    # Главный скрипт конвертера
│   └── README.md       # Документация
├── venv/               # Виртуальное окружение (не в репозитории)
└── .processing_log.json # База обработанных файлов (автоматически)
```

## 🎯 Возможности

✅ **Автоматическое отслеживание** - обрабатывает только новые и изменённые файлы  
✅ **Поддержка форматов** - PDF, DOCX, PPTX, DOC, XLSX, HTML  
✅ **Извлечение таблиц** - таблицы конвертируются в Markdown  
✅ **OCR** - распознавание текста на изображениях  
✅ **Быстрая обработка** - 10-30 секунд на документ  

## 📝 Использование

### Активировать виртуальное окружение

**В PowerShell:**
```powershell
Set-ExecutionPolicy Unrestricted -Scope Process
..\venv\Scripts\activate
```

**Для рабочего ноутбука:**
```powershell
.\venv\Scripts\activate
```

**Для Cursor:**
```powershell
& "C:\VScode\venv\Scripts\Activate.ps1"
```

### Запустить конвертер

```bash
python main_files/converter.py
```

Или из папки `main_files/`:
```bash
cd main_files
python converter.py
```

### Базовая конвертация

### Повторная обработка файла

1. Измените файл в `input/` (дата модификации обновится)
2. Запустите `python converter.py`

### Сброс базы обработанных файлов

Удалить базу (все файлы будут обработаны заново)
del .processing_log.json

## ⚙️ Что делает скрипт

1. Сканирует папку `input/`
2. Сравнивает с базой `.processing_log.json`
3. Обрабатывает новые и изменённые файлы
4. Сохраняет результаты в `output/`
5. Обновляет базу

## 🔧 Устранение проблем

### Ошибка: "Папка input не найдена"
mkdir input

### Ошибка: "Модели не загружены"
docling-tools models download

### Медленная первая обработка
Это нормально - модели загружаются (~700MB). Последующие запуски быстрые.

## 📊 Поддерживаемые форматы

| Формат | Расширение | Особенности |
|--------|-----------|-------------|
| PDF | `.pdf` | Таблицы, изображения, OCR |
| Word | `.docx`, `.doc` | Полная поддержка |
| PowerPoint | `.pptx` | Текст и таблицы |
| Excel | `.xlsx` | Таблицы |
| HTML | `.html` | Структура и текст |

## 🎓 Обучение

Это проект для изучения Docling от 0 до профи.  
Текущий этап: **Этап 2 завершён** ✅

---

Версия: 1.0  
Дата: 18.11.2024

## 📂 Полезные команды

### Просмотр структуры проекта
```powershell
# Список всех файлов и папок в main_files/
Get-ChildItem main_files -Recurse | Select-Object FullName, Length

# Структура main_files/
tree main_files /F
```


## 📚 Памятка Git 
Представьте, что Git — это служба доставки.
Ваш компьютер — это ваш дом.
GitHub — это центральный склад.
Часть 1. ОТПРАВКА (Из дома на склад)
Вы поработали с кодом (что-то исправили, дописали) и хотите сохранить это в истории.
Шаг 1. Сборка посылки (git add)
Вы говорите: "Вот эти файлы я хочу отправить".
Команда: git add .
(Точка означает "взять ВСЁ, что изменилось в этой папке").
Шаг 2. Упаковка и подпись (git commit)
Вы заклеиваете коробку скотчем и пишете на ней, что внутри. Без этого шага посылка никуда не поедет.
Команда: git commit -m "Описание того, что вы сделали"
Пример: git commit -m "Исправил gitignore для readme"
Важно: Обязательно пишите комментарий в кавычках.
Шаг 3. Отправка курьером (git push)
Посылка готова, но она все еще у вас в коридоре. Этой командой вы отправляете её на склад (GitHub).
Команда: git push
Если Git ничего не пишет в ответ или пишет "Everything up-to-date", значит, отправлять нечего (вы забыли сделать шаг 1 или 2).

Часть 2. ПОЛУЧЕНИЕ (Со склада домой)
Вы пришли на другой компьютер (или коллега что-то поменял), и вам нужно забрать свежие файлы.
Вариант А (Безопасный, если вы ничего не меняли)
Вы просто хотите скачать обновления.
Команда: git pull
(Эта команда сама скачает изменения и сразу обновит ваши файлы).
Вариант Б (Жесткий, если что-то пошло не так)
Если Git ругается, или вы хотите просто "сбросить" всё до состояния, как на сервере (как мы делали сегодня).
Скачать информацию: git fetch origin
Превратить файлы в точную копию сервера: git reset --hard origin/master